<br/>
Wait for the host to start.<br/>
Click the submit button as soon as you are told to do so.<br/><br/>
<%= form_for @attempt, url: '/quiz/playgame' do |f| %>
  <%= f.hidden_field :quiz_id %>
  <%= f.hidden_field :taker %>
  <%= f.hidden_field :current_question_number %>
  <%= f.hidden_field :phase %>
  <%= f.submit "Submit" %>
<% end %>


